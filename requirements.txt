# Keep in sync with PIP_REQUIREMENTS in main.py (used for auto-install on first run).
anthropic>=0.34.0
python-docx>=1.1.2
markdown-it-py>=4.1.0
mdit-py-plugins>=0.4.2
python-dotenv>=1.0.1
pypandoc_binary>=1.13
